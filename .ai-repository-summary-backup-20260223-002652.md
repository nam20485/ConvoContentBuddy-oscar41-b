# ConvoContentBuddy Repository Summary

> AI-powered background listening tool for real-time programming interview assistance.

## High-Level Overview

**ConvoContentBuddy** is a highly resilient, AI-powered application that processes live speech audio, identifies algorithmic problems being discussed, and retrieves optimal solutions via the Gemini API—all without requiring manual user input.

| Attribute | Value |
|-----------|-------|
| **Repository Type** | .NET 10 Solution (Aspire-orchestrated microservices) |
| **Languages** | C# 14, JavaScript (Interop), SQL |
| **Frameworks** | .NET 10, ASP.NET Core 10, Blazor WebAssembly 10, .NET Aspire 10 |
| **Target Runtimes** | .NET 10.0 (browser, linux, windows) |
| **Status** | Setup Phase - Solution structure pending creation |

## Build Instructions

> **Note:** The .NET solution (`ConvoContentBuddy.sln`) does NOT exist yet. It will be created in Phase 3 of the project plan.

### Prerequisites
- .NET SDK 10.0.100 (see `global.json`)
- Docker/Podman (for containerized services)
- Node.js (for utility scripts)

### Verification Commands (After Solution Creation)

```bash
# Build with Roslyn analyzer warnings as errors
dotnet build ConvoContentBuddy.sln -warnaserror

# Verify code style compliance
dotnet format ConvoContentBuddy.sln --verify-no-changes

# Run unit tests
dotnet test ConvoContentBuddy.sln --no-build
```

### Auto-fix Code Style

```bash
# Auto-format code (run if --verify-no-changes fails)
dotnet format ConvoContentBuddy.sln
```

## Project Layout

### Planned Solution Structure

```
ConvoContentBuddy.sln
├── ConvoContentBuddy.AppHost          # Aspire Orchestrator (entry point)
├── ConvoContentBuddy.ServiceDefaults  # OTLP, Health Checks, Resilience
├── ConvoContentBuddy.API.Brain        # ASP.NET Core Web API (Semantic Kernel Hub)
├── ConvoContentBuddy.UI.Web           # Blazor WASM (SignalR Client, Speech Interop)
└── ConvoContentBuddy.Data.Seeder      # Worker Service (LeetCode ingestion)
```

### Current Repository Structure

```
/
├── .devcontainer/           # Development container configuration
│   └── devcontainer.json    # Uses ghcr.io/nam20485/agents-prebuild:main-latest
├── .github/
│   ├── workflows/           # CI/CD pipelines
│   │   ├── prebuild.yml     # Dev container image builder
│   │   ├── opencode.yml     # OpenCode agent integration
│   │   ├── claude.yml       # Claude agent integration
│   │   ├── secret-scan-trufflehog.yml  # Secret scanning
│   │   └── validate-setup-scripts.yml  # Script validation
│   ├── ISSUE_TEMPLATE/      # Issue templates (epic, story, task)
│   └── .labels.json         # GitHub labels configuration
├── .githooks/               # Git hooks (pre-commit trufflehog scan)
├── .kilocode/               # Kilocode agent configuration
├── .qwen/                   # Qwen agent configuration
├── .claude/                 # Claude agent configuration
├── .workflow/               # Workflow automation scripts
├── docker/                  # Docker configuration for agents
├── local_ai_instruction_modules/  # Local AI instructions
│   ├── ai-dynamic-workflows.md
│   └── ai-workflow-assignments.md
├── plan_docs/               # Application specifications
│   └── New Application Spec_ ConvoContentBuddy.md
├── scripts/                 # Utility scripts (PowerShell)
│   ├── gh-auth.ps1          # GitHub CLI authentication
│   ├── import-labels.ps1    # Import GitHub labels
│   ├── create-milestones.ps1
│   └── security/            # Security utilities
├── global.json              # .NET SDK version (10.0.100)
├── AGENTS.md                # AI agent entry point
└── ConvoContentBuddy.code-workspace  # VS Code workspace
```

## Key Files

| File | Purpose |
|------|---------|
| `global.json` | Pins .NET SDK to version 10.0.100 with `rollForward: latestMinor` |
| `AGENTS.md` | Entry point for AI agent instructions; configures branch resolution |
| `plan_docs/New Application Spec_ ConvoContentBuddy.md` | Complete application specification and development plan |
| `.devcontainer/devcontainer.json` | Development container using pre-built image |
| `.github/workflows/*.yml` | CI/CD pipelines for build, test, and agent integrations |

## Architecture Highlights

### Technology Stack
- **AI/LLM:** Microsoft.SemanticKernel, Microsoft.Extensions.AI, Gemini 2.5 Flash
- **Data & State:** Qdrant (vector DB), PostgreSQL (pgvector), Redis (SignalR backplane)
- **Resilience:** Polly policies, Triple Modular Redundancy (TMR)
- **Real-time:** SignalR with Redis backplane for WebSocket failover
- **Styling:** Tailwind CSS

### Key Architectural Patterns
1. **Triple Modular Redundancy (TMR):** 3 replicated API instances for aerospace-grade resilience
2. **Hybrid Vector-Graph Search:** Qdrant semantic search + PostgreSQL graph traversal
3. **Zero-Interaction UI:** Blazor WASM with autonomous SignalR-driven updates
4. **Safe Mode Fallback:** Local-only operation when cloud services are unreachable

## CI/CD Workflows

| Workflow | Trigger | Purpose |
|----------|---------|---------|
| `prebuild.yml` | Push to main/development | Build dev container image |
| `opencode.yml` | Workflow dispatch | OpenCode agent integration |
| `claude.yml` | Workflow dispatch | Claude agent integration |
| `secret-scan-trufflehog.yml` | Push, PR | Detect secrets in code |
| `validate-setup-scripts.yml` | Push, PR | Validate PowerShell scripts |

## Development Workflow

This repository uses **dynamic workflows** orchestrated via the `AGENTS.md` entry point. Workflow assignments are fetched from the remote canonical repository (`nam20485/agent-instructions`).

### Branch Resolution
- Configured branch in `AGENTS.md` determines instruction source
- Default: `main` branch
- Raw URLs use `raw.githubusercontent.com` format

## Performance Targets

| Metric | Target |
|--------|--------|
| Semantic vector matching | < 500ms |
| End-to-end processing (Transcript → AI → UI) | < 2 seconds |
| Problem identification accuracy | ≥ 95% |
| Failover recovery | < 5 seconds |

## Related Documentation

- [Application Specification](./plan_docs/New%20Application%20Spec_%20ConvoContentBuddy.md)
- [Technical Design Document](./plan_docs/ConvoContentBuddy_%20Technical%20Design%20Document.md)
- [Implementation TODO List](./plan_docs/ConvoContentBuddy_%20Implementation%20TODO%20List.md)
